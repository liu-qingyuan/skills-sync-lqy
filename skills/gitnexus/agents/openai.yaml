interface:
  display_name: "GitNexus"
  short_description: "GitNexus CLI/MCP grounding for OMX"
  default_prompt: "Use $gitnexus to verify GitNexus CLI/MCP support and ground this task in the indexed code graph before planning or implementation."
dependencies:
  tools:
    - type: "mcp"
      value: "gitnexus"
      description: "GitNexus MCP server for indexed repository graph context"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
