# 领域文档

本仓库使用单 context 布局：

- 根目录 `CONTEXT.md`：领域术语和上下文。
- `docs/adr/`：架构决策。

开始相关工作前读取这些文件；文件不存在时安静地继续，不要预先创建。术语或决策真正明确后，由 `$domain-modeling-lqy` 等相关 skill 按需维护。

输出中的领域概念应使用 `CONTEXT.md` 定义的术语。若工作与现有 ADR 冲突，明确指出冲突，不要静默覆盖。
