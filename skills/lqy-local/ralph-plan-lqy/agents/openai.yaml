interface:
  display_name: "Ralph Plan LQY"
  short_description: "Plan or run a locked Ralph loop with a Pi worker."
  default_prompt: "Run the branch-aware Ralph backlog with Pi run_ralph. Plan only when asked; use locked Pi CLI if the tool is unavailable."
