interface:
  display_name: "Ralph Plan LQY"
  short_description: "Plan a branch-aware Ralph loop with a worktree lock."
  default_prompt: "Use $ralph-plan-lqy to create a branch-aware Open Ralph issue-backlog command with the Git contract, eligibility gate, per-worktree lock, prompt path, and monitoring notes."
